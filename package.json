{
  "name": "pikpak-auto-bangumi",
  "version": "0.1.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "start:cli": "bun run src/index.ts --mode cli",
    "start:server": "bun run src/index.ts --mode server",
    "test": "bun test",
    "db:generate": "bunx drizzle-kit generate",
    "db:migrate": "bunx drizzle-kit migrate"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/better-sqlite3": "^7.6.8",
    "drizzle-kit": "^0.30.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "drizzle-orm": "^0.45.2",
    "elysia": "^1.4.28",
    "fast-xml-parser": "^5.7.1",
    "zod": "^4.3.6"
  }
}
